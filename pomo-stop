#!/usr/bin/env bash
# pomo-stop
STATE="$HOME/bin/pomodoro/.pomo_state"
if [[ -f "$STATE" ]]; then
  rm -f "$STATE"
  echo "🛑 Pomodoro stopped."
  if command -v tmux >/dev/null 2>&1 && tmux info >/dev/null 2>&1; then
    tmux refresh-client -S
  fi
else
  echo "ℹ️ No active Pomodoro."
fi

